# 新功能开发计划

## 需求分析

### 1. 消息中心功能
- 在左侧tab栏中加入"消息中心"
- 实现站内信功能，可以接收系统消息
- 支持消息的查看、标记已读等基本操作

### 2. 家庭管理移动
- 将"家庭管理"从"设置"页面移动到左侧tab栏
- 保持原有功能不变

### 3. 数据库结构调整
- **重要变更**：一个用户最多只能属于一个家庭
- 删除bills表中的family_id字段
- 新建用户家庭关系表（实际上已存在family_members表，需要调整逻辑）
- 调整相关的前后端代码

### 4. 家庭管理增强
- "创建家庭"时支持"加入成员"功能
- 支持按登录名模糊查询用户
- 发送邀请消息给被邀请用户
- 用户可以接受或拒绝邀请

### 5. 退出家庭功能
- 用户可以在"家庭管理"中退出家庭
- 退出时需要确认提示框

### 6. 用户管理功能
- 在左侧tab栏加入"用户管理"
- 支持用户的增删改查操作

## 开发步骤

### 第一阶段：数据库设计和后端基础

#### 1.1 创建消息系统相关表
```sql
-- 消息表
CREATE TABLE messages (
    id SERIAL PRIMARY KEY,
    sender_id INTEGER REFERENCES users(id),
    receiver_id INTEGER NOT NULL REFERENCES users(id),
    message_type VARCHAR(50) NOT NULL, -- 'system', 'family_invitation', 'notification'
    title VARCHAR(200) NOT NULL,
    content TEXT NOT NULL,
    data JSONB, -- 存储额外数据，如邀请相关信息
    is_read BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 消息操作表（用于记录用户对消息的操作）
CREATE TABLE message_actions (
    id SERIAL PRIMARY KEY,
    message_id INTEGER NOT NULL REFERENCES messages(id),
    user_id INTEGER NOT NULL REFERENCES users(id),
    action_type VARCHAR(50) NOT NULL, -- 'accept', 'reject', 'read'
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

#### 1.2 调整用户家庭关系逻辑
- 修改family_members表，确保一个用户只能属于一个家庭
- 添加唯一约束：`ALTER TABLE family_members ADD CONSTRAINT unique_user_family UNIQUE (user_id);`

#### 1.3 删除bills表中的family_id字段
- 创建迁移脚本删除family_id字段
- 修改Bill模型，移除family_id相关代码
- 调整账单查询逻辑，通过用户的家庭关系获取家庭ID

#### 1.4 创建消息相关模型
- 创建Message模型
- 创建MessageAction模型
- 定义相关的Schema

#### 1.5 创建消息相关API
- 获取用户消息列表
- 标记消息为已读
- 处理家庭邀请（接受/拒绝）
- 发送系统消息

### 第二阶段：家庭管理功能增强

#### 2.1 用户搜索API
- 创建用户搜索接口，支持按用户名模糊查询
- 排除已在家庭中的用户

#### 2.2 家庭邀请功能
- 修改创建家庭API，支持同时邀请成员
- 创建发送邀请消息的服务
- 创建处理邀请响应的API

#### 2.3 退出家庭功能
- 创建退出家庭API
- 处理退出后的数据清理（如账单归属等）

### 第三阶段：用户管理功能

#### 3.1 用户管理API
- 用户列表查询（支持分页、搜索）
- 用户创建
- 用户信息更新
- 用户删除（软删除）
- 用户状态管理（启用/禁用）

#### 3.2 权限控制
- 确定用户管理的权限要求（是否需要管理员权限）
- 实现相应的权限检查

### 第四阶段：前端界面开发

#### 4.1 调整导航结构
- 修改Layout组件，添加新的菜单项
- 移动家庭管理到主菜单
- 添加消息中心和用户管理菜单

#### 4.2 消息中心页面
- 创建消息列表组件
- 实现消息详情查看
- 实现消息操作（标记已读、处理邀请）
- 添加消息提醒功能

#### 4.3 家庭管理页面重构
- 从设置页面移除家庭管理
- 创建独立的家庭管理页面
- 添加成员邀请功能
- 添加退出家庭功能

#### 4.4 用户管理页面
- 创建用户列表页面
- 实现用户搜索和筛选
- 创建用户编辑表单
- 实现用户操作（增删改查）

#### 4.5 调整现有页面
- 修改账单相关页面，适配新的家庭关系逻辑
- 更新相关的API调用

### 第五阶段：测试和优化

#### 5.1 单元测试
- 为新的API编写测试用例
- 测试数据库约束和业务逻辑

#### 5.2 集成测试
- 测试完整的邀请流程
- 测试家庭管理功能
- 测试用户管理功能

#### 5.3 前端测试
- 测试新页面的功能
- 测试导航和路由
- 测试用户交互流程

## 技术细节补充

### 数据库约束
1. 确保用户只能属于一个家庭：`UNIQUE(user_id)` on family_members
2. 消息系统的数据完整性约束
3. 软删除用户时的数据处理

### API设计原则
1. RESTful API设计
2. 统一的响应格式
3. 适当的HTTP状态码
4. 请求参数验证

### 前端状态管理
1. 使用Zustand管理消息状态
2. 实时消息提醒（可考虑WebSocket）
3. 缓存用户和家庭信息

### 安全考虑
1. 用户权限验证
2. 防止越权操作
3. 输入数据验证和清理

## 风险评估

### 高风险项
1. **数据库结构变更**：删除bills表的family_id字段可能影响现有数据
2. **用户家庭关系变更**：限制用户只能属于一个家庭可能影响现有用户

### 缓解措施
1. 在变更前备份数据库
2. 编写数据迁移脚本，妥善处理现有数据
3. 分阶段部署，先在测试环境验证

## 预估工期
- 第一阶段：3-4天
- 第二阶段：2-3天  
- 第三阶段：2天
- 第四阶段：4-5天
- 第五阶段：2天

**总计：13-16天**